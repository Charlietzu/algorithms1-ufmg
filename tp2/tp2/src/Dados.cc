#include "Dados.h"

Dados::Dados() {}

Dados::~Dados() {}

void Dados::ProcessaEntrada(vector<Aeroporto *> &aeroportos, vector<Rota *> &rotas)
{
    string entrada = "";
    int qtdAeroportos = 0, qtdRotas = 0;

    cin >> qtdAeroportos >> qtdRotas;

    
}